//- Mixin para mostrar estrellas de rating
mixin outputRating(rating)
    .rating
        - for (let i = 0; i < 5; i++)
            if i < rating
                i.fas.fa-star
            else
                i.far.fa-star